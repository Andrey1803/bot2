# Telegram Bot Token (обязательно)
API_TOKEN=ваш_токен_бота

# YouGile API Key (обязательно)
YOUGILE_API_KEY=ваш_api_ключ_yougile

# ID колонки в YouGile для создания задач (обязательно)
COLUMN_ID=id_колонки

# Telegram ID администратора (необязательно)
ADMIN_ID=123456789

# ID проекта YouGile (необязательно, для утилит)
PROJECT_ID=id_проекта

# Секрет для проверки вебхуков от YouGile (необязательно, но рекомендуется)
YOUGILE_WEBHOOK_SECRET=ваш_секрет_для_вебхука

# --- Интеграция с «Диспетчер задач» (опционально) ---
# Базовый URL API (без /v1/...), тот же хост, что у Fastify API, не фронт Next.js.
# Лучше с https://; если указать только хост — к нему добавится https:// автоматически.
# На сервере диспетчера обязательны: DISPATCHER_INBOUND_API_KEY (>=16 символов), DISPATCHER_INBOUND_OWNER_USER_ID (id пользователя-владельца).
DISPATCHER_API_URL=
# Тот же секрет, что DISPATCHER_INBOUND_API_KEY в переменных API диспетчера
DISPATCHER_INBOUND_API_KEY=
# Один из двух вариантов маршрутизации:
# 1) Напрямую в существующую группу
DISPATCHER_GROUP_ID=
# 2) Автоподбор/создание компании + группы (если GROUP_ID не задан)
DISPATCHER_COMPANY_NAME=
DISPATCHER_GROUP_NAME=Заявки Telegram
# Колонка в диспетчере: PRELIMINARY или OPEN (если пусто — решает API по умолчанию)
# DISPATCHER_INBOUND_INITIAL_STATUS=PRELIMINARY
# Плановое ТО в создаваемой задаче (поля maintenance* в inbound-order). Интервал по умолчанию 6 мес.
# DISPATCHER_MAINTENANCE_PLAN=1
# DISPATCHER_MAINTENANCE_INTERVAL_MONTHS=6
# Явная дата следующего ТО YYYY-MM-DD (если не задана — сегодня + интервал по серверу бота)
# DISPATCHER_MAINTENANCE_NEXT_DUE_YMD=
# DISPATCHER_MAINTENANCE_NOTE=Источник: Telegram
# Стабильный externalRef по адресу: tg:<id>|<хэш адреса> (для будущей синхронизации с учётом)
# DISPATCHER_EXTERNAL_REF_PER_ADDRESS=1

# При старте бота: автоматически создать в диспетчере карточки ТО для пользователей из users.json (разово на профиль,
# id задачи пишется в users.json как dispatcher_maint_task_id). По умолчанию 1; выключить: DISPATCHER_AUTO_SYNC_MAINTENANCE=0
# DISPATCHER_AUTO_SYNC_MAINTENANCE=1
# Одноразовый перенос вручную: scripts/import_bot_profiles_to_dispatcher.py
